# Novel Reader App - 統合テスト方法

このドキュメントでは、Novel Reader Appのテストスイートの実行方法と管理方法を説明します。

---

## 📋 目次

1. [テスト概要](#テスト概要)
2. [テストの種類](#テストの種類)
3. [環境セットアップ](#環境セットアップ)
4. [テスト実行方法](#テスト実行方法)
5. [テストファイル一覧](#テストファイル一覧)
6. [トラブルシューティング](#トラブルシューティング)
7. [CI/CD統合](#cicd統合)
8. [ベストプラクティス](#ベストプラクティス)

---

## テスト概要

### 統計情報

| カテゴリ | テストファイル数 | テスト数 | コード行数 |
|---------|----------------|---------|----------|
| ユニットテスト | 3 | 95 | 874 |
| 計装テスト（DAO） | 2 | 47 | 818 |
| 計装テスト（Repository） | 1 | 27 | 478 |
| 計装テスト（Migration） | 1 | 14 | 532 |
| **合計** | **7** | **183** | **2,702** |

### テストカバレッジ

| レイヤー | カバレッジ | 説明 |
|---------|----------|------|
| ユーティリティ | ~95% | Base62、PseudoNcodeGenerator |
| アダプター | ~80% | テキストクリーンアップ関数 |
| DAO | ~90% | NovelDescDao、EpisodeDao |
| Repository | ~85% | NovelRepository |
| Database Migration | 100% | v4→v11の主要マイグレーション |

---

## テストの種類

### 1. ユニットテスト (Unit Tests)

**場所:** `app/src/test/java/`
**実行環境:** ローカルJVM（実機/エミュレータ不要）
**実行速度:** 高速（10-20秒）

#### 対象ファイル:
- `Base62ConverterTest.kt` - Base62エンコード/デコードのテスト
- `PseudoNcodeGeneratorTest.kt` - カクヨムNcode生成/判定のテスト
- `KakuyomuTextCleanupTest.kt` - HTMLエンティティデコードのテスト

### 2. 計装テスト (Instrumented Tests)

**場所:** `app/src/androidTest/java/`
**実行環境:** Android実機またはエミュレータ
**実行速度:** 低速（2-5分）

#### 対象ファイル:
- `NovelDescDaoTest.kt` - 小説メタデータDAOのテスト
- `EpisodeDaoTest.kt` - エピソードDAOのテスト
- `NovelRepositoryTest.kt` - リポジトリ統合テスト
- `MigrationTest.kt` - データベースマイグレーションテスト

---

## 環境セットアップ

### 必要な依存関係

`app/build.gradle.kts`に以下が含まれていることを確認：

```kotlin
dependencies {
    // ユニットテスト
    testImplementation("junit:junit:4.13.2")
    testImplementation("org.jetbrains.kotlinx:kotlinx-coroutines-test:1.7.3")
    testImplementation("com.google.truth:truth:1.1.5")

    // Android計装テスト
    androidTestImplementation("androidx.test.ext:junit:1.1.5")
    androidTestImplementation("androidx.test.espresso:espresso-core:3.5.1")
    androidTestImplementation("androidx.room:room-testing:2.7.1")
    androidTestImplementation("androidx.work:work-testing:2.9.0")
    androidTestImplementation("com.google.truth:truth:1.1.5")
    androidTestImplementation("org.jetbrains.kotlinx:kotlinx-coroutines-test:1.7.3")
}
```

### エミュレータのセットアップ（計装テスト用）

計装テストを実行するには、Android実機またはエミュレータが必要です。

#### エミュレータの作成手順:

1. Android Studio → **Device Manager**（右上のアイコン）
2. **Create Device** をクリック
3. 推奨設定:
   - デバイス: Pixel 5
   - システムイメージ: API 34 (Android 14)
   - RAM: 2GB以上
4. **Finish** をクリック

---

## テスト実行方法

### Android Studioでの実行（推奨）

#### 1. 全テストの実行

```
app/ フォルダを右クリック
→ Run 'All Tests'
```

#### 2. ユニットテストのみ実行

```
app/src/test/ フォルダを右クリック
→ Run 'Tests in novel_reader.test'
```

**実行時間:** 約10-20秒

#### 3. 計装テストのみ実行

```
前提: エミュレータまたは実機を起動しておく

app/src/androidTest/ フォルダを右クリック
→ Run 'Tests in novel_reader.androidTest'
```

**実行時間:** 約2-5分

#### 4. 個別テストの実行

各テストファイルを開き、以下のいずれかを実行：

- **テストクラス全体:** クラス名の横の緑の▶ボタンをクリック
- **個別のテストメソッド:** メソッド名の横の緑の▶ボタンをクリック

#### 5. テスト結果の確認

- Android Studio下部の **Run** タブに結果が表示されます
- ✅ 緑 = 成功
- ❌ 赤 = 失敗
- 失敗した場合、スタックトレースをクリックして該当箇所にジャンプできます

### コマンドラインでの実行

プロジェクトルート（`Novel_reader/`）で以下のコマンドを実行：

#### 全ユニットテストの実行

```bash
./gradlew test
```

#### 特定のテストクラスの実行

```bash
./gradlew test --tests "*.Base62ConverterTest"
./gradlew test --tests "*.PseudoNcodeGeneratorTest"
```

#### 全計装テストの実行

```bash
# 事前準備: エミュレータまたは実機を接続
adb devices  # デバイスが認識されているか確認

./gradlew connectedAndroidTest
```

#### 特定の計装テストの実行

```bash
./gradlew connectedAndroidTest \
  -Pandroid.testInstrumentationRunnerArguments.class=\
  com.shunlight_library.novel_reader.data.dao.NovelDescDaoTest
```

#### HTMLレポートの生成

```bash
# ユニットテスト
./gradlew test
# → 結果: app/build/reports/tests/testDebugUnitTest/index.html

# 計装テスト
./gradlew connectedAndroidTest
# → 結果: app/build/reports/androidTests/connected/index.html
```

HTMLレポートをブラウザで開くと、詳細なテスト結果を確認できます。

---

## テストファイル一覧

### ユニットテスト

#### 1. Base62ConverterTest (27テスト)

**場所:** `app/src/test/java/.../utils/Base62ConverterTest.kt`

**テスト内容:**
- エンコード基本テスト（0, 1, 61, 62, Long.MAX_VALUE）
- デコード基本テスト
- ラウンドトリップ検証（小さい数値、大きい数値）
- BigIntegerエンコード/デコード（カクヨムwork ID対応）
- エラーケース（負の数、無効な文字、空文字列）
- エッジケース（全Base62文字、一貫性チェック）

**実行方法:**
```bash
./gradlew test --tests "*.Base62ConverterTest"
```

#### 2. PseudoNcodeGeneratorTest (30テスト)

**場所:** `app/src/test/java/.../utils/PseudoNcodeGeneratorTest.kt`

**テスト内容:**
- カクヨムNcode生成（"K"プレフィックス、Base62エンコード）
- work ID抽出（Pseudo-Ncode → work ID）
- ラウンドトリップ検証（小さいID、19桁ID）
- `isKakuyomuNcode()` 判定ロジック
- `isSyosetuNcode()` 判定ロジック
- エラーケース（無効なwork ID、無効なフォーマット）

**実行方法:**
```bash
./gradlew test --tests "*.PseudoNcodeGeneratorTest"
```

#### 3. KakuyomuTextCleanupTest (38テスト)

**場所:** `app/src/test/java/.../data/adapter/KakuyomuTextCleanupTest.kt`

**テスト内容:**
- `decodeHtmlEntities()` - &amp;, &lt;, &gt;, &quot;, &nbsp;, &yen;, &copy;
- `decodeNumericEntities()` - &#12354;（10進数）、&#x3042;（16進数）
- `decodeUnicodeEscapes()` - \u3042（Unicodeエスケープ）
- 統合テスト（複数のデコード処理の組み合わせ）
- エッジケース（不完全なエンティティ、無効なコードポイント）

**実行方法:**
```bash
./gradlew test --tests "*.KakuyomuTextCleanupTest"
```

### 計装テスト

#### 4. NovelDescDaoTest (22テスト)

**場所:** `app/src/androidTest/java/.../data/dao/NovelDescDaoTest.kt`

**テスト内容:**
- 挿入/取得/更新/削除の基本操作
- Flow発火の確認
- タグ検索機能（main_tag、sub_tag）
- お気に入り機能
- 最近更新された小説の取得
- マルチサイト対応（カクヨム、R18）

**実行方法:**
```bash
./gradlew connectedAndroidTest --tests "*.NovelDescDaoTest"
```

#### 5. EpisodeDaoTest (25テスト)

**場所:** `app/src/androidTest/java/.../data/dao/EpisodeDaoTest.kt`

**テスト内容:**
- 基本的なCRUD操作
- エピソード一覧のソート
- 既読状態の管理（個別、一括）
- しおり機能
- 読書進捗の管理
- Flow発火の確認

**実行方法:**
```bash
./gradlew connectedAndroidTest --tests "*.EpisodeDaoTest"
```

#### 6. NovelRepositoryTest (27テスト)

**場所:** `app/src/androidTest/java/.../data/repository/NovelRepositoryTest.kt`

**テスト内容:**
- Novel/Episode基本CRUD操作（DAO委譲確認）
- カクヨムエピソードマッピング機能
  - エピソードとマッピングの一括保存
  - マッピングIDの取得・逆引き
  - 削除時のマッピング連動削除
- 読書履歴機能
  - 読書位置の保存・更新
  - 最近読んだ小説の取得
  - 読書履歴の削除

**実行方法:**
```bash
./gradlew connectedAndroidTest --tests "*.NovelRepositoryTest"
```

#### 7. MigrationTest (14テスト)

**場所:** `app/src/androidTest/java/.../data/database/MigrationTest.kt`

**テスト内容:**
- v4→v5マイグレーション（reading_rate追加）
- v9→v10マイグレーション（episode_mappingテーブル追加）
- v10→v11マイグレーション（registered_at追加）
- v4→v11一括マイグレーション
- データ保持の確認
- スキーマ整合性の確認

**実行方法:**
```bash
./gradlew connectedAndroidTest --tests "*.MigrationTest"
```

---

## トラブルシューティング

### 問題1: テストが見つからない

**症状:**
```
No tests found
```

**解決方法:**
1. Android Studioで **File** → **Sync Project with Gradle Files**
2. **Build** → **Clean Project**
3. **Build** → **Rebuild Project**

### 問題2: 計装テストが実行できない

**症状:**
```
Error: No connected devices
```

**解決方法:**
1. エミュレータまたは実機が接続されているか確認
   ```bash
   adb devices
   ```
2. エミュレータを起動していない場合、Android Studio → Device Manager → エミュレータを起動
3. USBデバッグが有効になっているか確認（実機の場合）

### 問題3: MigrationTestが失敗する

**症状:**
```
MigrationTestHelper initialization failed
```

**解決方法:**
1. `build.gradle.kts`に`androidx.room:room-testing`が含まれているか確認
2. Gradle同期を実行
3. Clean & Rebuild

### 問題4: テストがタイムアウトする

**症状:**
```
Test timed out after 2 minutes
```

**解決方法:**
1. エミュレータのスペックを上げる（RAM 2GB以上推奨）
2. エミュレータを再起動
3. Android Studioを再起動
4. 個別テストを実行して問題を特定

### 問題5: Gradle ビルドエラー

**症状:**
```
Could not resolve dependencies
```

**解決方法:**
1. インターネット接続を確認
2. Gradle キャッシュをクリア
   ```bash
   ./gradlew clean --refresh-dependencies
   ```
3. Android Studioを再起動

---

## CI/CD統合

### GitHub Actionsの設定例

`.github/workflows/test.yml` を作成：

```yaml
name: Run Tests

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  unit-tests:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Grant execute permission for gradlew
        run: chmod +x Novel_reader/gradlew

      - name: Run unit tests
        run: |
          cd Novel_reader
          ./gradlew test

      - name: Upload test reports
        if: always()
        uses: actions/upload-artifact@v3
        with:
          name: test-reports
          path: Novel_reader/app/build/reports/tests/

  instrumented-tests:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v3

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Grant execute permission for gradlew
        run: chmod +x Novel_reader/gradlew

      - name: Run instrumented tests
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 34
          target: google_apis
          arch: x86_64
          script: |
            cd Novel_reader
            ./gradlew connectedAndroidTest

      - name: Upload test reports
        if: always()
        uses: actions/upload-artifact@v3
        with:
          name: instrumented-test-reports
          path: Novel_reader/app/build/reports/androidTests/
```

### Pre-commit Hookの設定

`.git/hooks/pre-commit` を作成：

```bash
#!/bin/bash

echo "Running unit tests..."
cd Novel_reader
./gradlew test

if [ $? -ne 0 ]; then
    echo "❌ ユニットテストが失敗しました。コミットを中止します。"
    exit 1
fi

echo "✅ ユニットテストが成功しました。"
exit 0
```

実行権限を付与：
```bash
chmod +x .git/hooks/pre-commit
```

---

## ベストプラクティス

### 1. テストの命名規則

```kotlin
// Good
@Test
fun insertNovel_and_getByNcode_returnsNovel() { }

// Bad
@Test
fun test1() { }
```

**命名パターン:** `<メソッド名>_<条件>_<期待結果>()`

### 2. テストの構造（AAA パターン）

```kotlin
@Test
fun exampleTest() {
    // Arrange (Given): テスト準備
    val novel = createTestNovel(ncode = "n1234ab")

    // Act (When): 実行
    repository.insertNovel(novel)

    // Assert (Then): 検証
    val retrieved = repository.getNovelByNcode("n1234ab")
    assertNotNull(retrieved)
}
```

### 3. テストの独立性

- 各テストは他のテストに依存しない
- テスト順序に依存しない
- `@Before`で初期化、`@After`でクリーンアップ

### 4. テストデータの管理

```kotlin
// ヘルパー関数を使用してテストデータを作成
private fun createTestNovel(
    ncode: String = "n1234ab",
    title: String = "テスト小説"
): NovelDescEntity {
    return NovelDescEntity(...)
}
```

### 5. 定期的なテスト実行

- **開発中:** 変更した機能のテストを実行
- **コミット前:** 全ユニットテストを実行
- **プルリクエスト作成前:** 全テスト（ユニット + 計装）を実行
- **リリース前:** 全テストを実行 + 手動テスト

### 6. テストカバレッジの目標

| レイヤー | 目標カバレッジ |
|---------|--------------|
| ユーティリティ | 90%以上 |
| DAO | 80%以上 |
| Repository | 80%以上 |
| ViewModel | 70%以上 |
| UI | 50%以上 |

---

## まとめ

このテストスイートは以下を保証します：

✅ **データの整合性** - マイグレーション時のデータ損失を防止
✅ **機能の正確性** - Base62エンコード、カクヨムマッピングなどの複雑なロジックを検証
✅ **リグレッション防止** - 既存機能の破壊を早期検出
✅ **リファクタリング支援** - 安心してコード構造を変更可能
✅ **ドキュメント化** - テストコードが使用例として機能

テストを定期的に実行し、コードの品質を維持しましょう！

---

**最終更新日:** 2025-01-27
**バージョン:** 1.0
**作成者:** Claude Code
